{
  "id": "browser-verifier-agent",
  "mode": "generated",
  "file": ".codex/agents/browser-verifier.toml",
  "expect_text": [
    "name = \"browser-verifier\"",
    "description = \"Delegated executor of shape's browser-verify slot:",
    "developer_instructions = '''",
    "verdict: PASS | DRIFT | BLOCKED | MISSING-TOOL",
    "Missing → return verdict `MISSING-TOOL` immediately; do not install anything.",
    "4. **Close — always.**",
    "agent-browser close",
    "Never inline base64 or image contents in the return — paths only."
  ],
  "reject_text": [
    "\nmodel = "
  ]
}
