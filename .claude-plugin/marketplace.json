{
  "name": "skills",
  "owner": {
    "name": "Paul",
    "url": "https://github.com/ChenPaulYu"
  },
  "plugins": [
    {
      "name": "nav",
      "source": "./plugins/nav",
      "description": "Keep any codebase navigable as it grows — audit (assess + feasibility), refactor (verbatim move + test gates), sync (navigation layer: file-top headers + bilingual codebase HTML, one grounding pass), plan (spec → grounded plan), do (execute a small behaviour-changing change, disciplined, no artifact). Language-agnostic. Built on Ousterhout's deep-module principles (deep modules via information hiding).",
      "version": "0.4.0"
    },
    {
      "name": "shape",
      "source": "./plugins/shape",
      "description": "Forward motion: give intent its form via real disposable instances (not descriptions), record decisions legibly, keep them current, and build them into running, verified code. Sibling to nav. Skills: mockup (render-to-decide), elicit (grounded grill → a thought), position (canon-authoring campaign → core docs), setup (scaffold a new project to a verified baseline — archetype references + stack principles), dogfood (use a built feature for real → friction + coverage gaps), align (decide next + render the plan), reconcile (prune stale notes), build (drive the plan's In-progress to done); doctor deferred. Language-agnostic.",
      "version": "0.5.0"
    },
    {
      "name": "research",
      "source": "./plugins/research",
      "description": "Argument anatomy for research work — dissect one or more documents into Gap/Claim/Mechanism/Evidence skeletons, untangle how a set of sources relate (lineage / clusters / contradictions / contested ground, with optional claim-framed positioning), critique a paper adversarially into a referee report (claim↔evidence audit + self-attack), or run a provenance audit over your own documents (trace every load-bearing citation back to a verified source; quarantine the unread).",
      "version": "0.5.0"
    },
    {
      "name": "think",
      "source": "./plugins/think",
      "description": "Reason about a problem through a named reasoning lens that forces a structure the default 'think harder' skips. first-principles: strip a question to its irreducible axioms, rebuild from them, surface where that diverges from convention. invert + second-order are the seed family. Feeds shape one-way; standalone.",
      "version": "0.1.0"
    }
  ]
}
