# shape — plugin-level CLAUDE.md

> Editing this plugin. To run a skill, read its `SKILL.md` (self-contained).

## What it is

**Forward motion: give intent its form, then hand it to the manual build path.** Sibling to `nav` — the two split the lifecycle cleanly: **shape pushes the work forward** (converge → plan), **nav keeps the result healthy** (audit → refactor → map). Domain = the loop **converge a decision → record it legibly → keep it current → hand off to be built**.

> Identity note (ADR-011, revised ADR-110): shape once widened to include `build`, an orchestrator driving the plan's In-progress column to done. It retired 2026-08-12 — zero fires ever, Paul walks the same loop by hand (`/nav:plan` → `/nav:do`/`/nav:refactor` → `/shape:align`). Re-entry condition: he asks for autonomous board-driven execution more than once.

## Spine (through-line)

**Converge by a real, disposable instance — never a description.** The instance's *form* varies: a verbal grounded fork (elicit), or a rendered interactive artifact (mockup). Its standing form is the blueprints board — **one current state, one maintained render**: `plan.md` (agent-facing; deps point downstream only). A human-facing view renders **on demand via `/shape:mockup`** when actually wanted — disposable, never a standing file to drift. Its **terminus is now the manual path** (ADR-110): the disposable instances converge the decision; the user drives it real by hand — `/nav:plan` grounds an item, `/nav:do`/`/nav:refactor` implements it, `/shape:align` moves it to Shipped. `build` orchestrated this loop autonomously and retired unexercised; its ADR-040 dispatch machinery survives in git for the re-entry condition above.

**Progressive disclosure runs through every artifact.** Every artifact leads with its interface and drills in only as needed — markdown structure (a `head -12`-able header + TL;DR, sections that lead with their point) for the agent-facing docs, click-to-reveal for any rendered mockup, including an on-demand board snapshot. Same interface-first principle as `nav`'s file headers.

**The verb is the only door for its deliverable (ADR-038).** If you are about to hand-produce something a verb encodes — a comparison mockup (`/shape:mockup`), a converge volley (`/shape:elicit`), a decided behaviour-changing change (`/nav:do`) — **fire the verb instead of hand-rolling the artifact**. The artifact is not the deliverable; the *protocol around it* is (post-pick offers, check bracket, gated diffs) — hand-rolling produces the artifact and silently drops the protocol. Having done the craft ambiently many times is the *risk signal*, not a waiver: fluency is exactly when the door gets skipped (two sightings: 2026-06-03 execution, 2026-06-11 converge). This does not make verbs auto-fire — it governs the case where you're *already producing* the deliverable.

**Decisions are born durable — no freeze tier (ADR-112).** There is no separate canon layer to graduate into and no ledger of pending amendments waiting on a freeze moment: a converged decision lands directly in `thoughts/` with a `Status:` line (`in force` / `superseded by <file>` / `shipped`), and a changed decision is just an edit to that line plus a fresh entry — made by whichever verb touches it (`elicit` for a reconsidered call, `align` for a fact reality already settled). Principles route by how they must fire, not by a tier: **always-loaded** (a project's CLAUDE.md, paid every turn), **a gate/validator** (mechanically enforced, can't silently drift), or **a dated case-bearing file** in `thoughts/` (situational, read on demand). `core/`, the ADR-041 freeze protocol, and `docs/core/amendments.md` retired with `/shape:position` — the tier conflated two genres (timeless principle vs. architecture that must track code), and freezing the second manufactured permanent drift debt: across 5 projects the `core/` trees were scaffolded then abandoned, one holding only a `.gitkeep` for 3 weeks while the repo shipped daily.

**Below 90% → ask (core principle, shared with `nav` rule ⑦).** When unsure about scope, boundaries, or intent, stop and ask — never guess. This is the spine of the whole family, not a per-skill add-on: `elicit` *is* this rule as a workflow (it converges by asking, not answering); `mockup` lets the user point; `align` decides *with* the user and gates every destructive step per-file. Any autonomous driver built on top runs **only while confidence holds**. The moment scope/boundary/intent drops below 90%, it stops and asks rather than plowing ahead — the cost of pausing to clarify is far smaller than unwinding a wrong guess.

## Members

Grouped by verb (mirrors `nav`'s family shape):

- **converge** — turn an open question into a decision:
  - `mockup` — render-to-decide: a real interactive HTML (UI mockup, or diagram for backend/agent/data/flow). *(built)*
  - `elicit` — draw the decision out by a grounded fork (react-not-author, drill-to-principle, compress-to-one-line; weight-adaptive exit — stop on the snap; summoned, not auto) → a `thoughts/` doc. **Gatekeeper (ADR-076):** when a volley stalls, diagnoses which of three stalls it is (fuzzy-but-known → keep grilling; missing terrain → run the **survey leg** in place; unmeasured fact → offer `/shape:probe`) — the ruling never leaves elicit. The survey leg (folded from the retired `survey` door, ADR-110) maps the decision space before converging: two layers (domain-prior axes, borrowing `frame:orthogonal`'s independence check *by protocol* + a reality-diff layer grounded in the repo at `file:line`), then reports the **diff** between the user's stated understanding and the full map — the diff is the deliverable, teaching is a service on it. Small gaps explained in place; big gaps offer `/frame:analogize` or `deep-research` — offers, never calls. Full leg protocol: `plugins/shape/skills/elicit/references/survey-leg.md`. *(built)*
  - `probe` — design and run a **minimal experiment** for the fourth knowledge quadrant: a fork nobody can settle by argument (not the user, not the model, not the world's documented knowledge). Design chain borrowed *by protocol*, in sequence: `frame:dialectic` (locate the load-bearing assumption) → `frame:first-principles` (strip it to the smallest testable claim) → `frame:orthogonal` (control the variables — its independence check *is*, verbatim, a controlled experiment's definition). Three canonical shapes: A/B comparison, blind judgment test, behavior probe. Pre-registers the verdict rule before the first trial runs; design + verdict-reading stay session-model, execution legs dispatchable to cheap-tier agents (ADR-067); its own live-LLM-cost signal instance for paid-LLM fan-out (ADR-062). Read-only toward product code — only the findings doc persists. Lands `docs/findings/` (if present) or `blueprints/thoughts/…-probe.md`, tolerant-reader compliant (ADR-071); evidence feeds the pending decision back to elicit or the user. Summoned directly, or the destination of `/frame:dialectic`'s guarded closing offer (a frame→shape soft offer, ADR-012 pattern). *(built; ADR-075)*
  - `dogfood` — use a *built* feature that feels unsmooth for real: drive the REAL interface (`agent-browser` / `curl` / CLI) against a list of user intents, report **friction** (works but clunky → a UX idea) + the **coverage gaps** that fall out (an intent with no path → a logic hole). Experience-first; logic-coverage is the byproduct. Output is a friction report (not a mandatory mockup); render is an optional redesign hand-off. A gap's layer (missing intent = direction · dead-end = incomplete) routes it, handed to `elicit`. Borrows `/verify`·`/run`'s drive-the-real-app method (asks "is it smooth + what's missing", not "is it correct"). *(built; ADR-033, supersedes ADR-020)*
- **project** — keep the record honest, then decide what's next:
  - `align` — one pass that leaves `blueprints/` telling the truth about **now**: **verify** every carried item against the code first, mechanically rather than by re-reading the board (ADR-086 — a real sweep once found 5 of 7 "Next" items had already shipped), then **compact** what drifted (amend a stale fact in place, prune/consolidate a wholly-stale doc, retire a shipped mockup once its pick is recorded in the owning doc — the three ADR-037 preconditions), then **triage** now/next/later *with the user* and write `plan.md` (the agent's single maintained board). No carried item vanishes silently — everything lands Shipped, Next, Future, or an explicit user-confirmed cut. Runs on **compaction pressure** (back from a break, a batch just shipped, the tree got noisy), not a schedule. A human wanting to see it renders an on-demand snapshot via `/shape:mockup` — align never maintains a standing HTML file. Folds the former `reconcile` verb into this one pass (ADR-112): the record's honesty and the next call were always read off the same evidence, so one door replaces two. The pre-build mirror of `/nav:audit` + careful `/nav:refactor`. *(built; verify-before-triage ADR-086, mockup retirement ADR-037, reconcile merge ADR-112)*
- **maintain** — keep the tree's format current:
  - `migrate` — upgrade a tree to the current **convention version**: detect which dialect `blueprints/`/`core/` speaks, propose the mapping, then execute **verbatim, gated, reference-safe** structural transforms and verify nothing dangles (e.g. v1 `decisions.md` → v2 `precedents/`). The convention is a versioned interface with living instances, so an append-only ledger of transforms ships with each convention change — without it a convention change either orphans the fleet or freezes forever. Moves recorded content; never re-decides it (that's `elicit`) and never judges staleness (that's `align`). Distinct from `/relay:migrate` (GitHub workspace migration). **Summon-only** (`disable-model-invocation: true`, ADR-110) — an episodic, author-driven event with zero model-routing value; capability intact behind `/shape:migrate`. *(built; ADR-105)*
- **`blueprints/` = convention**, not a skill — the artifact container the verbs read/write. `thoughts/` carries decisions permanently (dated, `Status:` line — in force / superseded by \<file\> / shipped); there is no separate `core/` tier to graduate into (ADR-112 retired it along with `/shape:position`). Layout + `plan.md` shape + the on-demand human-render contract (rendered by `/shape:mockup`, never maintained as a standing file — ADR-063) live in [`skills/align/references/blueprints-spec.md`](plugins/shape/skills/align/references/blueprints-spec.md).
- **`doctor` = deferred** — an orchestrator (elicit → align) only crystallizes once there's genuinely enough to orchestrate — an even thinner sequence than before the ADR-112 merge. nav's own `doctor` was **retired** (ADR-021) for being too thin a sequence (`audit → sync`, 2 steps) — a caution that an orchestrator with little to orchestrate is redundant ceremony, not a convenience. Don't build shape's until the sequence earns it.

There is deliberately **no `init` skill** — scaffolding the `blueprints/` tree folds into `align`'s first run (ask location once, create idempotently). A skill for `mkdir -p` would be structure-theatre.

## The seams with `nav` (hard deps one-way; soft recommendations either way)

shape is the forward-motion half; `nav` is the maintenance half. The dependency rule has two tiers:
- **Hard deps (import / call / breaks-without-it) are one-way: shape → nav.** `nav` must run fully standalone — never *depend* on shape.
- **Soft recommendations (a guarded *offer* / hand-off that degrades if absent) may go nav → shape.** These already exist and are fine: `/nav:plan` Stage 2 hands visual ambiguity to `/shape:mockup`, and Stage 4 *offers* a `/shape:mockup` summary (guarded — omitted if shape isn't installed). See ADR-012.

Record the seams; don't blur them:

1. **`blueprints/` is the hand-off artifact to `/nav:plan` — both directions.** shape converges intent into blueprints; `nav:plan` grounds a thought/spec into a code-level implementation plan **and writes it back into `blueprints/plans/`** when the tree is present (soft `nav → shape` preference, ADR-017). `align` triages forward; `nav:plan` grounds one item down — adjacent verbs, not overlapping. The grounded plan stays co-located so the whole arc (decision → status → grounded-how) lives in one tree.
2. **`align` consumes the file headers `/nav:sync` maintains, and maintains `plans/` + `mockups/`.** `head -12` file headers make "is this implemented?" answerable cheaply — the strongest staleness signal, and the evidence align's verify-before-triage pass runs on (ADR-086). align keeps **`thoughts/`, `plans/`, and `mockups/`** current (a plan whose steps all shipped is stale like an implemented thought; a shipped decision's mockup retires once its residue is absorbed — ADR-037).
3. **The build loop is now manual, not a concentrated seam.** `build` used to *control* the loop (lives in shape, forward-motion) while *calling* nav's code-side protocols per item; it retired unexercised (ADR-110). The user now walks the same sequence by hand: `/nav:plan` (ground) → `/nav:do`/`/nav:refactor` (implement) → `/shape:align` (writes back). Direction stays one-way: shape → nav.

## Conventions for skills inside this plugin

> Repo-wide **authoring + maintenance** rules (naming, skills-root-relative paths, stack-neutral examples, frontmatter `description`, ADR-on-new-skill, the site-map gate, versioning) live in the repo-root [`CLAUDE.md`](CLAUDE.md). Shape-specific:

- **★ Progressive disclosure in every produced artifact** (so an agent scans fast, not just the human): a `thoughts/` doc opens with `# <title> — <one-line role>` + a ≤3-line TL;DR/status blockquote (`head -12` yields the gist without reading the body), sections lead with their point, enumerations go in tables, key terms bolded; a `mockup` artifact opens with a top-comment stating what it is · the candidates · the pick. This is `nav`'s interface-first (rule ②) applied to shape's outputs. Every skill that emits a doc/artifact (`elicit`, `mockup`, `align`) enforces it.
- **Write-gated**: `align` writes files — show what will be written (or a diff) before committing. Its destructive ops follow the safety rules in its `SKILL.md` (check tracked/untracked, never `mv`-then-`rm`, diff before delete).
- **Skills don't invoke each other**: they reference sibling protocols by name (e.g. align → "run `/shape:elicit`" for a decision-change it finds mid-pass), never re-implement or call them.
- **Cost tier (ADR-058/059)**: the *criterion* (mechanical sweep/format/render → `model: sonnet` frontmatter, turn-level) is owned by the repo-root [`CLAUDE.md`](CLAUDE.md); shape's tiered verb is **`align`**. Judgment-heavy verbs (`elicit`) stay on the session model. The same tier powers the browser-verify slot's `browser-verifier` subagent (below).
- **Live-LLM-cost signal (ADR-062)**: a different axis than the cost tier above — it flags the cost of the **feature being dogfooded/verified**, not the skill's own model/tokens, when that feature itself calls a live paid LLM (fan-out/multi-agent especially). Root definition + `nav:do`'s inheritance chain (`plan`, `refactor`) live in nav's [ADR-062](docs/adr/062-live-llm-cost-signal.md). shape's own instance (not inherited — drives the real thing its own way): `dogfood`'s live-LLM-cost caveat.

## browser-verify — a shared per-project capability slot

`mockup` needs to *see* the running thing (render the artifact, including an on-demand board snapshot) — the slot's one live consumer since `build`'s verify-against-mockup step retired (ADR-110); it stays a shared, pluggable capability rather than mockup-specific code, since a future re-entrant orchestrator would need the same slot (N+1 trigger, in reverse):

- **Default implementation: `agent-browser`** (vercel-labs/agent-browser) — a browser-automation CLI. Detect: `which agent-browser`. Usage: `agent-browser open <url>` → `agent-browser screenshot <file>.png`; `snapshot -i` / `click @e` / `fill @e "..."` to interact.
- **Don't hardcode the tool — name the capability.** A skill describes the contract ("open the running feature → screenshot → compare to mockup → **close when done**"); `agent-browser` is the named **default**, not the contract. The close is *part of* the contract, not optional cleanup: an unclosed headless browser silently burns a core after the pass returns, and the cost lands on the user's machine — invisible from inside the loop, so it has to be in the contract, not left to judgment (trap #3). Caveat: this written contract is *polled* knowledge, not a *cue* — it fires only if the executing path actually walks it. So bind the close where open *executes*: write each verify step so its last move is `close` (a walked step is a cue, a reference doc is not), and lean on a turn-end browser reaper as the cross-path backstop. The doc raises the odds it's in-context; it does not make it fire.
- **If missing, fail helpfully — never silently skip.** Surface a 3-way confidence-gate choice: **(a) install** [recommended] — `npm install -g agent-browser` (or `brew`/`cargo install agent-browser`) then `agent-browser install`; or as a skill `npx skills add vercel-labs/agent-browser`. **(b)** proceed without visual verify this run (flag items to eyeball). **(c)** skip verify for this item. Report what was skipped (no silent caps).
- **Per-project override:** a project may bind a different helper (Playwright, puppeteer) in its own CLAUDE.md; the agent reads that. Absent an override, the default is agent-browser.
- **Skills don't call skills:** name the capability + describe the contract; the executing agent invokes the tool (CLI shell-out, or triggers the agent-browser skill in its own turn).
- **Delegate the pass to the `browser-verifier` subagent — the cost tier (ADR-058).** The slot's default *executor* is the plugin's [`agents/browser-verifier.md`](plugins/shape/agents/browser-verifier.md) (model: sonnet). The caller injects URL · screenshot destination · expectation (mockup path or stated behaviour) · any interaction steps / tool override, and receives back a compact verdict (`PASS | DRIFT | BLOCKED | MISSING-TOOL` + reason + paths) — the drive-and-capture loop and its **image tokens stay in the subagent's context**, never entering the main conversation (where every screenshot is re-paid on every later turn). Delegate whenever the pass is mechanical (mockup's capture confirmation, including a board-snapshot render); drive inline only when the user is being walked through the page live. The close-contract binds inside the subagent — its last move is `close`. Ships with the plugin, so it works on any machine that installed shape.
- **Verify economy — capture at decision points, not per step.** A screenshot is *evidence* (a pick's record, friction-evidence), never a progress note: `dogfood` screenshots at friction/gap moments and records video **only on explicit request**; `mockup` confirms a render once, not per iteration (a board snapshot included). Screenshots live on disk (report/mockups tree) — referenced by path, not pasted into the chat.
- **Known traps:** the slot's field manual ([`references/browser-verify-gotchas.md`](plugins/shape/references/browser-verify-gotchas.md)) is organized by scope — **universal** (session isolation · assert state-not-text · measure-before-toggle · selector-over-pixel clicks · poll-don't-snapshot · rule-out-mock-data · daemon-reset · teardown/`close` with its knowledge≠trigger cue-ladder + provenance-keyed reaper), **audio** (dual-backend mute · real-input for user-activation), **transform-canvas** (`position:fixed` → portal to body), and **React+Vite** (controlled-input native-setter, HMR console ghosts). Read before trusting a screenshot or console scan, and `close` the browser when the pass ends.

## Status

Six skills built (`mockup`, `elicit`, `probe`, `dogfood`, `align`, `migrate`); `doctor` (orchestrator) deferred until the family proves out. `setup` — scaffold-to-verified-baseline — was retired as never invoked, its archetype-scaffolding payload not a door anyone reached for; see [ADR-107](docs/adr/107-retirement-round-four-verbs.md). Down from eight per [ADR-112](docs/adr/112-one-board-verb-two-tiers.md): `reconcile` merged into `align` as one verify → compact → triage pass (the record's honesty and the next call were always read off the same evidence, so one door replaces two), and `position` retired along with it — the `core/` canon tier, the ADR-041 freeze protocol, and the amendments ledger all go with it; a converged decision now lands directly in `thoughts/` with a `Status:` line, born durable, with no freeze/graduation step to wait on. Down from ten per [ADR-110](docs/adr/110-shape-slims-to-eight.md) before that: `survey` folded into `elicit` as its survey leg (formerly Phase 1 of the elicit-ecosystem plan — mid-grill terrain-mapping now lives inside elicit's own gatekeeper, offered when a volley stalls on missing terrain), `build` retired (zero fires ever; the manual `/nav:plan` → `/nav:do`/`/nav:refactor` → `/shape:align` path is the terminus now), `migrate` demoted to summon-only. `probe` designs and runs a minimal experiment for a fork nobody can settle by argument — summoned directly, or the destination of `frame:dialectic`'s guarded closing offer (ADR-075), Phase 2 of the same plan; kept as a door on user attestation (ADR-110). `elicit`'s behavioral spec comes from `docs/observations/2026-05-29-thought-mode-how-paul-converges.md`; the browser-verify slot lands in ADR-011; blueprints drops the standing `overview.html` for an on-demand `/shape:mockup` render in ADR-063. History: `docs/observations/2026-05-29-shape-plugin.md`.
